#host used for connection to a database
host = "localhost"

#port used for connection to a database
port = 5432

#name of user for access to a database
user = "postgres"  

#password for access to a database
password = "password" 

#name of a database
database = "webserver"